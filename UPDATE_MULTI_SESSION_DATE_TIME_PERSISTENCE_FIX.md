# 多日期活動日期／時間保留修正

- 多日期活動時段一律以 Asia/Hong_Kong 時區處理。
- `session_date` 與 `start_at` / `end_at` 儲存前重新組合，避免多次編輯後跨日。
- 建立與更新 API 均檢查時段日期一致性。
- 編輯既有活動時，自動由現有 sessions 推算已儲存節數區段（開始、結束、間隔、名額、節數）。
- 同一日不同間隔會拆成不同已儲存區段，可點擊載入修改。
- 不更改資料庫 schema，無需 migration。
