# 多時段活動：節數間隔快速產生

後台多時段活動的每個活動日期加入「按節數間隔快速產生時段」。

- 可輸入開始時間、結束時間、節數間隔（分鐘）及每節名額。
- 同一日期可重複使用不同區段及不同間隔，例如：
  - 12:00–14:00，每 15 分鐘一節
  - 14:00–16:00，每 30 分鐘一節
- 系統只在時間區段可以完整除盡節數間隔時產生，避免最後出現不完整節數。
- 與沒有參加者的既有時段重疊時，會以新節數取代。
- 與已有參加者的時段重疊時會阻止操作，避免影響報名資料。
- 原有手動新增及逐節編輯時段功能保留。
- 不需要資料庫 migration；產生後仍使用現有 event_sessions。
