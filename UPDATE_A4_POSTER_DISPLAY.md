# A4 activity poster display update

The activity detail and registration pages now display the complete uploaded poster without cropping.

- Uses `object-fit: contain` and centered positioning.
- Supports portrait A4 artwork while preserving its original proportions.
- Keeps the existing activity text, registration form layout, positions, and component dimensions unchanged.
- Empty space around non-matching image ratios uses a neutral light background.
