# 多日期／多時段活動更新

- 後台新增「建立多時段活動」入口。
- 每個活動可自由建立不同日期、開始／結束時間及獨立名額。
- 舊有單時段活動完全保留。
- 首頁多時段活動顯示名額狀況：開放報名（綠）、反應熱烈（橙）、尚餘少量（紅）。
- 前台報名時先選擇日期及時段，並顯示每時段剩餘名額。
- 參加者名單按時段分欄，可拖放參加者至另一時段，並保留新增、修改及刪除。
- 資料庫 trigger 會同步活動總人數及各時段人數，滿額時阻止新增或搬動。

部署前請套用 `supabase/migrations/202608050001_add_multi_session_events.sql`。
